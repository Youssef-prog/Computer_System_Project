Useful command from the Makefile:
- "make files" : To open all the necessary files for understand the C code.
- "make test" : To compile and generate the ASM code.



Warning:

Group of instructions NO: 

- a = b;
- a = b + c;
- int a = b;

The group of instruction NO do not work if these instructions are not called at the beginning of a condition loop before any other instructions.
